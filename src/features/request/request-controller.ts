/** Imported modules */
import { RequestHandler } from "express";

/** Get the request */
const getRequest: RequestHandler = (_req, _res, _next) => {};

/** Create a new request */
const createRequest: RequestHandler = (_req, _res, _next) => {};

/** Update an existing request */
const updateRequest: RequestHandler = (_req, _res, _next) => {};

/** Delete a request */
const deleteRequest: RequestHandler = (_req, _res, _next) => {};

export { getRequest, createRequest, updateRequest, deleteRequest };
