/** Imported modules */
import { RequestHandler } from "express";

/** Get the collection */
const getCollection: RequestHandler = (_req, _res, _next) => {};

/** Create a new collection */
const createCollection: RequestHandler = (_req, _res, _next) => {};

/** Update an existing collection */
const updateCollection: RequestHandler = (_req, _res, _next) => {};

/** Delete a collection */
const deleteCollection: RequestHandler = (_req, _res, _next) => {};

export { getCollection, createCollection, updateCollection, deleteCollection };
